# Server

Run the backend server (wrapper at project root):

1. Copy `server/.env.example` to `server/.env` and edit values.
2. From project root run:

```bash
npm install
npm run start
```

The server boots `server/index.js` and requires a reachable MySQL instance.
